# Shukku List
A shared shopping list web app built with Firebase and vanilla JS.

## Setup
1. Create a Firebase project
2. Enable Firestore and Email/Password Auth
3. Replace config in `firebase-config.js`
4. Serve locally or deploy using Firebase Hosting

## Features
- Sign up / login
- Add, view, and sync shopping items in realtime
